# Copy this file to .env and customize for your environment

# Set to 'true' for mock mode (hardcoded responses, no Ollama needed)
# Set to 'false' for real mode (uses Ollama phi3:mini model)
USE_MOCK=true

# Ollama API endpoint (used when USE_MOCK=false)
OLLAMA_HOST=http://localhost:11434
